#include <stdio.h>
int main() {
	//each char is a numeric value
	//TODO: Please replace ??? in line 7 with an integer(not a char) so that your code builds and prints out ch = * 
	//Please only change ???
	
	int _ch = 42; // 42 correspond to * in the ASCII value
	printf("ch = %c\n", _ch);	
	return 0;
}


